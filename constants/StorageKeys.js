export const STORAGE_KEYS = {
    TASKS: '@dailyTasks',
    GOALS: '@goals',
    STREAKS: '@streaksHoursData_v2',
    // Legacy keys (kept for reference or migration if needed)
    LEGACY_STREAKS: '@streakData',
    // Auto-backup keys
    AUTO_BACKUP_ENABLED: '@autoBackupEnabled',
    AUTO_BACKUP_DIR_URI: '@autoBackupDirUri',
    LAST_AUTO_BACKUP_TIME: '@lastAutoBackupTime',
};
