/**
 * Constants — Barrel export for all constants.
 */

export { Colors } from './Colors';

export { STORAGE_KEYS } from './StorageKeys';

export {
    DAYS_PER_WEEK,
    DEFAULT_GOAL_WEEKS,
    STREAK_WIN_THRESHOLD_HOURS,
    STREAK_IDEAL_HOURS_PER_DAY,
    STREAK_MAX_AVG_HOURS_TARGET,
    TIER_CONSISTENCY_MAX_POINTS,
    TIER_INTENSITY_MAX_POINTS,
    TIERS,
    CONGRATS_MESSAGES,
    BACKUP_VERSION,
    APP_IDENTIFIER,
} from './Config';
